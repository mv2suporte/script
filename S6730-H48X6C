sysname "{{ device.name }}"
#
stp mode rstp
stp timer forward-delay 500
stp timer max-age 1000
stp instance 0 root primary
stp converge fast
#
vlan 3994                                 
 name gerencia
!
interface Vlanif3994
 ip address "{{ device.primary_ip4.address }}"
#
ip route-static 0.0.0.0 0.0.0.0 "{{ custom_field.gateway_ip }}" description Gateway-Default
